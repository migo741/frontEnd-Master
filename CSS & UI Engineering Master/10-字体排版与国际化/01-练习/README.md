# 练习：不会因语言和字体崩坏的文档页

## 题 1（60 分钟）

实现中英混排知识页：标题、正文、引用、代码、表格、长 URL、金额。320px、宽屏、200% 字号下保持合理 measure 和层级；只有代码/表格自身可横向滚动。

加入德文与阿拉伯文 fixture，用同一 CSS 支持 RTL，不复制布局。

## 题 2（60 分钟）

给品牌字体做加载方案，将同环境 5 次测试的字体相关 CLS 中位数降到 `< 0.05`。比较 system font、完整字体、subset variable font 三种方案，提交 waterfall、文件大小和布局证据。

