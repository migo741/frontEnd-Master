# 练习：工具栏与定价卡

## 题 1（55 分钟）

实现后台筛选工具栏：标题、结果数、搜索框、两个筛选器、批量操作和主按钮。宽屏单行，窄屏按任务顺序换行；搜索框可伸缩，主操作不被压扁，德文按钮与 200% 缩放仍可用。

禁止 absolute、固定总宽、JS 测宽和复制移动版 DOM。

## 题 2（45 分钟）

三张套餐卡文案和功能数不同，CTA 需视觉上靠底对齐，推荐 badge 不改变内容起点。增删功能无需改 CSS；320px 到宽屏保持 DOM 阅读顺序。

写出为什么这题内部适合 Flex、卡片集合更适合 Grid。

