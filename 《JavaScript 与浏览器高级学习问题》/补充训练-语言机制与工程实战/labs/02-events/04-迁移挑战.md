# 02 · 迁移挑战

任选其一，并保留现有回归。

- 加异步 callback：提供新的 emitAsync，明确串行/并行、失败聚合、取消以及最后一个 Promise 何时结算。不要偷偷改变原 emit 的同步契约。
- 加有界队列：容量256，定义满时拒绝、丢弃或按业务键合并的政策。给出队列峰值证据，解释哪些业务事件绝不能丢。
- 把模块接入03自动保存页面，用 saved/conflicted 等事件通知旁边的状态面板；面板反复安装/卸载100次后资源回到基线。

交付一张原契约/新契约对照表、实现和至少3个新增测试。
