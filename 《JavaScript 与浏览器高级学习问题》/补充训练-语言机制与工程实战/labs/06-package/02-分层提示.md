# 06 · 分层提示

1. Map迭代顺序可以表达LRU；命中后delete再set让它移到末尾。
2. hit形状解决undefined值和miss的歧义。不要写if(!value)判断命中。
3. expiration保存在entry里，与LRU顺序分开。get只改变使用顺序，set才更新过期时刻。
4. 独立consumer要消费解包后的文件，而非回到原源码目录使用相对路径导入，否则无法发现files/exports缺陷。
5. 导入无副作用的检查只覆盖本题可见的globalThis变化和有限进程行为；公开测试没有证明所有副作用都不存在，还要阅读代码检查timer/I/O。
