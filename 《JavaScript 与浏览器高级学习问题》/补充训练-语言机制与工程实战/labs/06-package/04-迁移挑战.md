# 06 · 迁移挑战

1. 新增peek，不改变LRU顺序。通过公共入口导出，证明旧consumer行为仍兼容。
2. 扩展成异步请求缓存：同key请求合并，定义拒绝是否缓存、取消属于哪个消费者、最后一个消费者离开后是否取消底层请求。
3. 容量扩到10万：用证据判断prune是否成为瓶颈，尝试过期堆/时间轮并维护复杂度和内存预算。

保留原版consumer用例，新增迁移说明和一个失败的兼容性测试，再进行修复。不要为展示高级程度而无需求地加CJS或构建器。
