# 04 · 迁移挑战

1. 两个并行Worker：最多2个在途批次，但输出仍按输入顺序。给慢任务排第一的用例，证明重排缓冲有上限。
2. 改用TypedArray加transfer：证明发送方buffer被detached，明确谁拥有数据，取消后如何释放；不要transfer仍由UI复用的buffer。
3. 不支持背压的推送源：定义满载时按实体合并、拒绝或重同步策略。解释为什么仅包装成AsyncIterable不能凭空获得端到端背压。

每次只改一个约束，记录吞吐、队列峰值和取消行为。保留基本题回归。
