# 03 · 迁移挑战

先选一个，再增加复杂度。

1. **防止草稿丢失**：open 遇到 dirty 时拒绝切换并返回明确状态，由UI确认；已经开始的请求如何处理必须写清楚。
2. **请求超时**：给 transport 加超时与资源清理。分别测试遵守 signal 和忽略 signal 的实现，说明“调用方不再等待”与“底层真的结束”的差别。
3. **离线恢复**：把 draft 与待提交意图写入 IndexedDB，保留服务端版本。恢复时用显式冲突政策，不将旧文本直接覆盖服务端。

至少补4条测试/操作序列。可将本模块接到旧课的工单项目，不需要重新实现框架或后端。
