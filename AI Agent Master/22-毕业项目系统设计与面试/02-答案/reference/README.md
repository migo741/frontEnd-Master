# 运行

```bash
python test_solution.py
python generate_eval_dataset.py
```

这是一条可测试的安全纵向切片，不是假装完整生产系统。正式提交还要把前面各章实现组合进来：PostgreSQL/queue/outbox、真实模型与工具 adapter、150+ 有真实输入/期望的 eval、traces/dashboard、ADR、威胁模型、runbook、部署与回滚脚本。

