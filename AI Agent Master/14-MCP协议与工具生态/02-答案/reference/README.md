# 运行

```bash
python test_solution.py
pip install "mcp[cli]>=1,<2"
python mcp_server.py
```

TypeScript contract client 需安装 `@modelcontextprotocol/sdk`，并从本目录执行。离线单元测试验证的是 transport 无关安全内核，因此不依赖网络或模型 API。

