# 练习：企业级 Combobox 与演进策略

## 题 1（90 分钟）

设计远程多选 Combobox：输入防抖、请求竞态、键盘导航、IME 中文输入、loading/empty/error、选中项删除、虚拟列表。只需实现状态机和关键 API，但必须写 8 个状态转换和 6 个可访问性断言。

## 题 2（45 分钟）

组件库要把 `Button type="danger"` 迁移为 `variant="solid" tone="danger"`，已有 600 个调用。设计兼容、告警、codemod、版本发布和指标计划；说明何时删除旧 API。

