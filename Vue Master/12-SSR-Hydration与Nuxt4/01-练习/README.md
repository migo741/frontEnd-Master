# 练习：定位水合错误与跨请求泄漏

## 题 1（45 分钟）

页面同时使用随机 id、`toLocaleString()`、浏览器宽度分支、在模块顶层创建 Pinia。列出每项症状、最小复现、正确修复。不得以 `<ClientOnly>` 包住整个页面作为答案。

## 题 2（60 分钟）

设计 Nuxt 的工单详情数据路径：服务端首屏取数、客户端不重复 fetch、切换 id 可取消、404/403 正确状态码、用户私有数据不进公共缓存、刷新 mutation 后重新验证。给缓存 key 和时序。

